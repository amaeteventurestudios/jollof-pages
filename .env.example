# Jollof Pages Environment Variables
# Copy this file to .env.local and fill in real values

# Auth (prototype only — no real auth implemented yet)
# NEXT_PUBLIC_DEMO_MODE=true
# AUTH_SECRET=your-secret-here

# Story OS integration (future)
# STORY_OS_PATH=./story-os
# STORY_OS_WRITE_ENABLED=false

# Agent configuration (future)
# AGENT_API_KEY=your-agent-api-key
# AGENT_ENDPOINT=https://api.anthropic.com/v1

# Database (future — no DB yet)
# DATABASE_URL=postgresql://...
